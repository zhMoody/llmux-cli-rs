# LLMux 配置模板 —— 复制为 .env 使用（.env 已被 gitignore）

# 网关/仪表盘端口
#   线上：25975
#   测试：25999
PORT=25975

# 数据目录（数据库文件 <DATA_DIR>/llmux_db.db）
#   线上默认：~/.config/llmux
#   测试：~/.config/llmux-repair
# DATA_DIR=/Users/moody/.config/llmux-repair

# 主密钥（不设则自动生成并存入 <DATA_DIR>/master.key）
# MASTER_KEY=

# 用量日志保留天数（启动时清理超过该天数的 usage_logs）
# USAGE_RETENTION_DAYS=30
